def insertion_sort_steps(lista):
    # Seu código aqui
    return None

print("Example:")
print(insertion_sort_steps([3, 1, 2]))

# As funções assert são utilizadas para verificar se o resultado da função chamada é o esperado

assert insertion_sort_steps([3, 1, 2]) == [[1, 3, 2], [1, 2, 3]]
assert insertion_sort_steps([4, 3, 2, 1]) == [[3, 4, 2, 1], [2, 3, 4, 1], [1, 2, 3, 4]]
assert insertion_sort_steps([1, 2, 3]) == []

print("Seu código passou pelos testes, clique em Check para avançar!")
